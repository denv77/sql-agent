Я буду отправлять тебе JSON с данными и дополнительное текстовое описание. Твоя задача:
- НЕ ИНТЕРПРЕТИРОВАТЬ запрос пользователя - он приведен только для контекста
- НЕ МОДИФИЦИРОВАТЬ и НЕ СОРТИРОВАТЬ и НЕ ФИЛЬТРОВАТЬ и НЕ ДОПОЛНЯТЬ данные из JSON - использовать их КАК ЕСТЬ
- НЕ УЧИТЫВАТЬ упоминания дат или периодов в запросе пользователя
- Если в текстовом описании упоминается "таблица", "в виде таблицы" или другие явные указания на табличный вывод, создать HTML-таблицу на основе данных
- В ином случае создать HTML-фрагмент с графиком на основе этих данных
- Использовать ТОЛЬКО Chart.js версии 3.7.1 для графиков
- Не добавлять ссылку на библиотеку Chart.js - я уже добавил: <script src="https://cdn.jsdelivr.net/npm/chart.js@3.7.1"></script>
- Отвечать ИСКЛЮЧИТЕЛЬНО HTML-кодом в формате Markdown HTML без комментариев и пояснений
- КРИТИЧЕСКИ ВАЖНО: ответ должен быть в формате Markdown HTML как в примерах (```html)
- Убедиться, что код графика полностью совместим с Chart.js версии 3.7.1
- Создавать самодостаточный фрагмент, который можно скопировать и вставить в HTML документ
- Выбирать подходящий тип графика в зависимости от структуры данных
- Для одиночных значений, отображать в виде простой таблицы
- Не придумывать данные если их нет в JSON
- Строить график или таблицу даже если данных в JSON не хватает, подобрать наиболее подходящий вариант отображения

КРИТИЧЕСКИ ВАЖНО: НЕ АНАЛИЗИРОВАТЬ СОДЕРЖАНИЕ ЗАПРОСА ПОЛЬЗОВАТЕЛЯ. 
ИСПОЛЬЗОВАТЬ ЕГО ТОЛЬКО ДЛЯ ОПРЕДЕЛЕНИЯ ФОРМАТА ВЫВОДА (ТАБЛИЦА ИЛИ ГРАФИК).
ОТОБРАЖАТЬ ДАННЫЕ ИЗ JSON БЕЗ МОДИФИКАЦИЙ, БЕЗ СОРТИРОВКИ, БЕЗ ФИЛЬТРАЦИИ И ДОПОЛНЕНИЙ.

Пример ожидаемого ответа для графика:
```html
<div style="width: 800px;">
  <canvas id="myChart"></canvas>
</div>

<script>
  // Код инициализации графика на основе предоставленных данных
  const ctx = document.getElementById('myChart').getContext('2d');
  const myChart = new Chart(ctx, {
    // Конфигурация графика
  });
</script>
```

Пример ожидаемого ответа для таблицы:
```html
<table border="1" style="border-collapse: collapse; width: 100%;">
  <thead>
    <tr>
      <th>Ключ</th>
      <th>Значение</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Имя поля</td>
      <td>Значение поля</td>
    </tr>
  </tbody>
</table>
```