<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Прототип AI</title>
    <script src="https://cdn.jsdelivr.net/npm/chart.js@3.7.1"></script>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        
        body {
            background-color: #f5f7fa;
            color: #333;
            display: flex;
            flex-direction: column;
            align-items: center;
            min-height: 100vh;
            padding: 2rem;
        }
        
        header {
            margin-bottom: 3rem;
        }
        
        h1 {
            font-size: 2.5rem;
            font-weight: 700;
            color: #2d3748;
            text-align: center;
            margin-top: 2rem;
        }
        
        .container {
            width: 100%;
            max-width: 800px;
            background-color: white;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
            padding: 2rem;
            margin-bottom: 30px;
        }
        
        form {
            display: flex;
            flex-direction: column;
            gap: 1.5rem;
        }
        
        .input-group {
            display: flex;
            flex-direction: column;
            gap: 0.5rem;
        }
        
        label {
            font-weight: 600;
            color: #4a5568;
        }
        
        input[type="text"] {
            padding: 0.8rem 1rem;
            border: 2px solid #e2e8f0;
            border-radius: 8px;
            font-size: 1rem;
            transition: border-color 0.3s;
        }
        
        input[type="text"]:focus {
            border-color: #4299e1;
            outline: none;
        }
        
        button {
            background-color: #4299e1;
            color: white;
            border: none;
            border-radius: 8px;
            padding: 0.8rem 1.5rem;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            transition: background-color 0.3s;
        }
        
        button:hover {
            background-color: #3182ce;
        }
        
        @media (max-width: 640px) {
            .container {
                padding: 1.5rem;
            }
            
            h1 {
                font-size: 2rem;
            }
        }

        .btn-submit:disabled {
            opacity: 0.7;
            background-color: #cccccc !important; /* Серый фон */
            color: #666666 !important; /* Тёмно-серый текст */
            cursor: not-allowed;
            border-color: #bbbbbb !important;
        }

        table { border-collapse: collapse; width: 100%; margin-top: 20px; }
        th, td { border: 1px solid #ddd; padding: 8px; }
        th {
            background-color: #f2f2f2;
            text-align: left;
            cursor: pointer;
        }
        .pagination {
            margin-top: 20px;
            display: flex;
            align-items: center;
            gap: 10px;
        }
        .pagination button { padding: 5px 10px; }

    </style>
</head>
<body>
    <header>
        <h1>Прототип AI</h1>
    </header>
    
    <div class="container">
        <form action="http://46.148.205.148:9000/query-DjbdKsjfbkjs" method="GET"
            onsubmit="document.querySelector('button[type=submit]').disabled=true; document.querySelector('button[type=submit]').innerHTML='Загрузка &bull;&bull;&bull;';">
            <div class="input-group">
                <label for="request">Введите ваш запрос:</label>
                <input type="text" id="request" name="request" placeholder="Что вы хотите узнать?" required>
            </div>
            <button class="btn-submit" type="submit">Отправить</button>

           
        </form>
    </div>

